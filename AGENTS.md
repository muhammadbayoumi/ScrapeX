# ScrapeX

The rules for changing this code live in [CLAUDE.md](CLAUDE.md). Read all of them before
you edit anything here.

This file is a pointer, not a second copy. There is no second copy: a duplicate of those
rules committed under another name is a second source of truth, and it drifts the moment
either side is edited — the first one written in this repository already carried a path
to a directory that does not exist.
